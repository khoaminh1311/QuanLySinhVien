<Window x:Class="Management_system.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Management_system"
        xmlns:fa="clr-namespace:FontAwesome.WPF;assembly=FontAwesome.WPF"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6FA">
    <Grid Margin="20,20,19.6,9.2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Center" Text="Đăng nhập hệ thống"
                   FontSize="20" FontWeight="Bold"
                   Foreground="#C70000" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="10">
            <!-- USERNAME -->
            <TextBlock Text="Đăng nhập:" FontWeight="Bold" Foreground="#444"/>
            <TextBox x:Name="txtUsername" Height="30" Margin="0,5,0,10" TextChanged="TxtUsername_TextChanged" Background="#FAFAFA" BorderBrush="#DDDDDD" />

            <!-- PASSWORD -->
            <TextBlock Text="Mật khẩu:" FontWeight="Bold" Foreground="#444"/>
            <PasswordBox x:Name="txtPassword" Height="30" Margin="0,5,0,10" Background="#FAFAFA" BorderBrush="#DDDDDD" />

            <!-- BUTTON -->
            <Button Content="Đăng nhập" Height="35"
                    Background="#C70000" Foreground="White"
                    FontWeight="Bold"
                    Click="Login_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#C70000"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8" Padding="10" >
                                        <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                        <!-- Hover -->
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E00000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#B00000"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
