<Page x:Class="InterfaceSinhVien.NotificationDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Chi tiết thông báo" Background="White">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="btnBack" Click="BtnBack_Click" Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,20"
                Background="Transparent" BorderThickness="0" Cursor="Hand" Foreground="#0078D7" FontSize="14">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⬅" Margin="0,0,5,0"/>
                <TextBlock Text="Quay lại danh sách" FontWeight="SemiBold"/>
            </StackPanel>
        </Button>

        <TextBlock x:Name="txtTitle" Grid.Row="1" Text="Tiêu đề thông báo ở đây" 
                   FontSize="24" FontWeight="Bold" Foreground="#D32F2F" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="📅 Đăng ngày: " Foreground="#888" FontSize="13"/>
            <TextBlock x:Name="txtDate" Text="..." Foreground="#555" FontSize="13" FontStyle="Italic"/>
        </StackPanel>

        <Border Grid.Row="3" BorderBrush="#EEE" BorderThickness="0,0,0,1" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="txtContent" Text="..." FontSize="16" LineHeight="28" 
                       TextWrapping="Wrap" Foreground="#333" TextAlignment="Justify"/>
        </ScrollViewer>
    </Grid>
</Page>